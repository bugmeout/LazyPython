今晚只是贴了一些代码。
lptwd一点都不懂，template中的render也不懂。

下午用lptwd的时候，发现不会报错。在linux下调试的时候发现又正常了。

然后回到windows上，又正常了。

有人能解决问题，我连问题是什么都发现不了。

关于linux：
1.gedit文件后，如果不关闭，在terminal下cat会使gedit卡死，force quit
2.web app只能开启一个，ctrl+z关闭后，不能再次运行。解决办法，就是关掉terminal然后再开一个。
3.文件修改了不会动态加载，需要关掉。

贴个问题贴，明天中午回来看看，哎，太挫了，一晚什么都没干，这个是真的。
pip install software.
是不是先下载安装包，解压，再安装，然后删除了这个安装包。
第二，我怎么知道pip下载的安装包在哪里，而且我怎么软件最后安装到了哪里。它的默认路径在哪里。
-------------------------------------------
在字典中查找属性的方法：
1.if “属性(index_name)” in dic
2.if dic.has_key("属性(index_name)")
-------------------
 